#!/bin/sh

tar xzf node-<%= version %>-linux-<%= arch %>.tar.gz
cd node-<%= version %>
./configure
make install

if [ $? != 0 ]; then
  mail -s 'Failed to build nodejs on <%= fqdn %>' <%= admin_email %> < /dev/null
fi
